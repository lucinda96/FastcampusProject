<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fastcampus.board.mapper.UserProfileMapper">
    <select id="getUserProfile" resultType="com.fastcampus.board.dto.UserDTO">
        SELECT
            id,
            userId,
            password,
            nickName,
            createTime,
            isWithDraw,
            status
        FROM user
        WHERE userId= #{id}
    </select>

    <insert id="insertUserProfile" parameterType="com.fastcampus.board.dto.UserDTO">
        INSERT INTO user (userId, passWord, nickName, isWithDraw, status, isAdmin)
        VALUES (#{userId}, #{password}, #{nickName}, #{isWithDraw}, #{status}, #{isAdmin})
    </insert>

    <update id="updateUserProfile" parameterType="com.fastcampus.board.dto.UserDTO">
        UPDATE user
        SET password=#{password},
            nickName=#{nickName},
            isWithDraw=#{isWithDraw},
            status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="deleteUserProfile">
       DELETE
       FROM user
       WHERE userId=#{id}
    </delete>

    <select id="findByIdAndPassword" resultType="com.fastcampus.board.dto.UserDTO">
        SELECT
            id,
            userId,
            password,
            nickName,
            createTime,
            isWithDraw,
            status
        FROM user
        WHERE userId = #{id}
            AND passWord = #{password}
            AND status != 'DELETE'
    </select>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE userId = #{id}
    </select>

    <update id="updatePassword" parameterType="com.fastcampus.board.dto.UserDTO">
        UPDATE user
        SET password=#{password}
        WHERE userId = #{userId}
    </update>

</mapper>